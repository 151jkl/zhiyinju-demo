# 知音局 Demo 实施计划

## 目标

在不要求普通用户申请 AccessSecret 的前提下，完成一个可现场演示、可解释、能体现知乎 API 能力的社区连接产品原型。

## 已完成

- 关系网络首页：中心用户、兴趣群落、用户节点和连接线。
- 节点交互：点击节点查看关系成立原因。
- 同频用户匹配卡片和个性化破冰提示。
- 观点圆桌和我的观点画像页面。
- Node 服务端代理知乎站内搜索、热榜和直答接口。
- AccessSecret 仅保存在服务端 `.env`，前端不暴露。
- “连接知乎”入口已支持 OAuth 配置：有赛事 App ID/Key 时走真实授权回调，没有配置时保留演示模式。
- 无密钥时自动切换演示数据，保证现场演示不中断。
- 核心匹配叙事改为“基于点赞、收藏、关注痕迹找同频者”，而不是泛兴趣标签。
- 匹配卡加入活跃度标签，优先推荐近期仍在知乎出现的人，降低“找到但联系不上”的落差。

## 本轮完善

1. **真实内容注入**：页面操作时请求知乎站内搜索；服务端保留热榜代理，作为实时话题来源。
2. **连接体验**：用户先连接主页，再进入关系地图；节点点击给出可解释关系线索。
3. **稳定性**：所有外部调用由服务端完成，统一处理错误、时间戳和演示回退。
4. **提交安全**：`.env` 加入 `.gitignore`，提交包只保留 `.env.example`。

## 比赛演示流程

1. 打开首页，说明“连接不是认识更多人，而是遇见想继续聊的人”。
2. 点击“连接知乎”，输入昵称和公开主页链接。
3. 点击“换一张地图”，展示真实知乎搜索请求和关系网络刷新。
4. 点击一位节点，展示“为什么连接成立”。
5. 进入观点圆桌，展示从内容消费到讨论关系的延伸。

## 后续可替换点

- 配置赛事 OAuth App ID/Key 后，联调真实回调并展示昵称、头像、创作、关注和近期收藏。
- 使用真实用户点赞、收藏数据替换本地行为画像；完整点赞历史需以知乎实际授权范围为准。
- 增加 Redis 缓存和调用配额统计，避免触发官方限制。
- 使用直答 Agent 对搜索结果生成圆桌摘要和破冰问题。
