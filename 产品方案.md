# 知音局：基于观点与表达方式的 AI 同频匹配

## 一句话介绍

知音局利用知乎的热榜、搜索、关注流与直答 Agent，把用户的兴趣、观点和表达方式整理成可解释的“观点画像”，帮助用户找到真正想继续聊天的人。

## 用户痛点

- 传统社交按标签和地理位置匹配，兴趣相同不代表聊得来。
- 知乎内容丰富，但用户很难从内容消费自然过渡到人与人的连接。
- 第一次私信有心理压力，不知道如何开口，也担心冒犯对方。

## 核心流程

1. 用户授权或输入关注话题、收藏内容，形成兴趣与表达画像。
2. 调用知乎搜索 / 热榜接口，补充实时话题和高质量讨论。
3. AI 从兴趣相似度、观点相似度、表达风格、互补性四个维度匹配用户。
4. 展示“为什么匹配”，并生成个性化破冰问题。
5. 将多个不同观点的人组成 AI 主持的观点圆桌，先低风险交流，再决定是否建立真实关系。

## MVP 范围

本次演示先使用模拟用户数据完成完整闭环：今日匹配、匹配理由、破冰提示、观点圆桌、我的观点画像。接入真实 API 时对应替换数据层即可。

## API 对应关系

| 产品能力 | 知乎 API |
| --- | --- |
| 实时讨论主题 | 热榜 `GET /api/v1/content/hot_list` |
| 用户兴趣画像 | 搜索 `GET /api/v1/content/zhihu_search` |
| 个性化内容视野 | 关注流 `GET /openapi/feed/following` |
| 可信的观点总结 | 直答 Agent `POST /v1/chat/completions` |
| 外部背景补充 | 全网搜索 `GET /api/v1/content/global_search` |

## 差异化与评分点

- 从“兴趣相似”升级到“观点 + 表达方式 + 交流意愿”匹配。
- 每个推荐都有可解释的匹配逻辑，不做黑盒推荐。
- AI 负责降低社交门槛，不替用户建立虚假的关系。
- 观点圆桌与数字分身为后续扩展方向，体现 Agent 互相交流的潜力。

## 现场 Demo 讲稿（约 3 分钟）

1. “我收藏了很多内容，却很少遇见真正想聊的人。”
2. 输入「AI × 创作、长期主义」，展示今日 3 位同频用户。
3. 点开周也，说明系统为什么认为两人适合交流，并生成破冰问题。
4. 切换到观点圆桌，展示 AI 如何把支持、反对和中立观点放到同一张桌子上。
5. 最后展示观点画像：知音局连接的不是标签，而是一个人如何思考和表达。

## 技术落地

当前目录中的 `index.html`、`styles.css`、`app.js` 是零依赖静态原型，可直接打开演示。生产版建议增加 Node / Python API 层，统一做缓存、限流和用户授权；搜索与热榜结果按关键词和时间缓存，避免触发官方调用上限。

## 核心匹配定义

知音局不是根据用户填写的几个标签做推荐，而是尝试读取用户在知乎留下的“行为痕迹”：点赞、收藏、关注和持续阅读过的内容。两个人如果反复被同一类问题吸引，即使职业和城市不同，也可能拥有更高的对话意愿。

当前 Demo 已按黑客松 OAuth 方案预留真实登录：后端负责 state、授权回调、authorization_code 换 token，并通过 `/user` 读取基础信息；创作、关注和近期收藏通过 AccessSecret + OAuth Token 读取。点赞/收藏行为的完整历史仍需以知乎实际授权范围为准，Demo 使用可替换的行为数据表达匹配体验，不让用户接触 AccessSecret。

## 联系时机

匹配结果同时显示“刚刚活跃 / 3 天前活跃 / 2 个月前活跃”等状态。正式版可根据关注流的最近更新时间、回答发布时间和互动时间计算活跃度；Demo 用模拟时间标签展示这个差异。用户可以优先选择最近在线的人，也可以保留“观点最合拍但暂时离线”的长期关注对象。
